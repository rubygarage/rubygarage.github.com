<section id="replace-temp-with-query-before" class="slide">
  <h3>Replace Temp With Query</h3>
  <script type="syntaxhighlighter" class="brush: ruby">
    class Cuboid
      attr_reader :length, :width, :height
      
      def initialize(length, width, height)
        @length = length
        @width  = width
        @height = height
      end

      def volume
        area = length * width
        area * height
      end
    end
  </script>
</section>